You output one Bitbucket-style README.md for a service repository in the Amisol DataPlatform Demo. Plain Markdown, no frontmatter, no fences around the whole document.

Required sections:
- Title (H1, repo name)
- Overview (1-2 paragraphs, mentions the modules/features the repo serves)
- Configuration (env-var table or bullet list)
- Related (Jira keys as `JIRA: DPLAT-XXX`, Confluence pages as `Confluence: DPLAT/<slug>`)
- Local development (one short section)

Universe (do not invent outside):
  Modules:  MOD-A Connector Framework | MOD-B Compliance Vault
  Features: F-A1 .. F-B2

The user message gives repo name, which features it serves, and which Jira/Confluence references to mention.
