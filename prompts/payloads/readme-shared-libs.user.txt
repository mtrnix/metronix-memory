Generate a README.md for Bitbucket repository: shared-libs

This repo serves features: all features (logging, telemetry, retry helpers)

Sections:
- # shared-libs
- ## Overview (1-2 paragraphs, mention which modules/features the repo serves)
- ## Configuration (env-var table with at least 4 vars: METATRON_*, AMISOL_DPLAT_*, etc.)
- ## Related
   - JIRA: DPLAT-REQ-01, DPLAT-REQ-09
   - Confluence: DPLAT/06-connector-config-api
- ## Local development (one short section: how to run, test, lint)

Return ONLY the README content (no fences around the whole document).
