# Hermes integration (compatibility page)

The supported Hermes MCP onboarding guide is now
[`hermes-agent.md`](hermes-agent.md). It contains the current connection settings, verification
steps, and troubleshooting guidance.

This compatibility page remains so existing bookmarks and installer output keep working. For
prompt templates generated by `install.sh`, see the adjacent [`hermes/`](hermes/) directory.
