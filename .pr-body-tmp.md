## Summary

- Reorganizes public documentation around install-first flow: updated `README.md`, `install.md`, `connecting_to_agent.md`, and `docs/README.md` with Metronix Memory branding and runtime guide index.
- Adds 20+ integration guides (Hermes, OpenClaw, Cursor, Claude Desktop, OpenCode, SDKs, n8n, etc.) with consistent Prerequisites / Setup / Verify / Troubleshooting sections, plus `docs/deployment.md` and example walkthroughs (`docs/examples/`).
- Aligns docs with the codebase: host API port `8001`, internal `metatron-core:8000`, `METATRON_*` env vars, `metatron_*` MCP tools; adds CHANGELOG `[1.0.0]`, CONTRIBUTING doc template, and compatibility shims for legacy integration links.

## Test plan

- [ ] Review README install steps against `docker-compose.full.yml` (ports, service names)
- [ ] Spot-check integration guides for correct MCP URL (`http://localhost:8001/mcp`) and `METATRON_MCP_API_KEY`
- [ ] Verify internal doc links in `docs/README.md` resolve (no broken paths to deleted `manual.md`)
- [ ] Confirm `connecting_to_agent.md` and `prompts.md` reference `metatron_*` tool names from `src/metatron/mcp/tools/`
- [ ] Run doc link scan: `rg` for `localhost:8000` in user-facing host URLs (should be `8001` where applicable)
