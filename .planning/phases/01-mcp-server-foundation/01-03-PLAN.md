---
phase: 01-mcp-server-foundation
plan: "01-03"
type: gap_closure
wave: 1
depends_on: []
files_modified:
  - src/metatron/mcp/server.py
  - src/metatron/api/app.py
autonomous: true
requirements:
  - TRNS-02
  - TRNS-03
  - AUTH-01
gap_closure: true
title: "Gap Closure: HTTP Transport Fixes"
must_haves:
  - HTTP transport uses correct FastMCP method (streamable_http_app, not http_app)
  - FastAPI mounts MCP using correct method  
  - Auth middleware is applied to HTTP transport
---

<objective>
Fix HTTP transport and FastAPI mounting issues discovered in verification.
</objective>

<context>
@.planning/phases/01-mcp-server-foundation/01-VERIFICATION.md
@.planning/phases/01-mcp-server-foundation/01-01-SUMMARY.md
@.planning/phases/01-mcp-server-foundation/01-02-SUMMARY.md
</context>

<gap_context>
From VERIFICATION.md, 3 gaps blocking TRNS-02 and TRNS-03:

1. **server.py line 119**: Uses `mcp.http_app()` which doesn't exist in FastMCP
   - Missing: Replace with `mcp.streamable_http_app()`

2. **api/app.py line 120**: Same issue with `mcp_server.http_app()`
   - Missing: Replace with `mcp_server.streamable_http_app()`

3. **AuthMiddleware not applied**: Defined in run_http() but never added to app
   - Missing: Actually wire up the auth middleware

Evidence:
- FastMCP has `streamable_http_app()` method, not `http_app()`
- AuthMiddleware class exists in server.py lines 129-145 but is unused
</context>

<tasks>

<task type="auto">
  <name>Fix HTTP transport method in server.py</name>
  <files>src/metatron/mcp/server.py</files>
  <action>
Replace `mcp.http_app()` with `mcp.streamable_http_app()` on line 119.

The FastMCP class provides `streamable_http_app()` method, not `http_app()`.

Current (broken):
```python
app = mcp.http_app(
    transport="streamable-http",
    stateless_http=True,
)
```

Fix to:
```python
app = mcp.streamable_http_app(
    transport="streamable-http",
    stateless_http=True,
)

# Add authentication middleware
app.add_middleware(AuthMiddleware)
```

Run verification after fix:
  PYTHONPATH=src .venv/bin/python -c "
import asyncio
from metatron.mcp.server import mcp
app = mcp.streamable_http_app()
print('HTTP app created:', type(app))
print('Routes:', [r.path for r in app.routes][:3])
"
  </action>
  <verify>HTTP app can be created without AttributeError</verify>
  <done>Server can create HTTP transport app using streamable_http_app()</done>
</task>

<task type="auto">
  <name>Fix FastAPI MCP mount in api/app.py</name>
  <files>src/metatron/api/app.py</files>
  <action>
Replace `mcp_server.http_app()` with `mcp_server.streamable_http_app()` on line 120.

The method doesn't exist - must use streamable_http_app() instead.

Verify after fix:
  PYTHONPATH=src .venv/bin/python -c "
from metatron.api.app import create_app
app = create_app()
print('FastAPI app created with MCP mount')
"
  </action>
  <verify>FastAPI app can be created without AttributeError</verify>
  <done>FastAPI app mounts MCP at /mcp without errors</done>
</task>

</tasks>

<verification>
Run verification tests:
1. PYTHONPATH=src .venv/bin/python -c "from metatron.mcp.server import mcp; app = mcp.streamable_http_app(); print('OK')"
2. PYTHONPATH=src .venv/bin/python -c "from metatron.api.app import create_app; app = create_app(); print('OK')"
3. Check that both transports work
</verification>

<success_criteria>
- [ ] HTTP transport uses streamable_http_app (not http_app) in server.py line 119
- [ ] API app uses streamable_http_app in api/app.py line 120
- [ ] AuthMiddleware is added to HTTP app in server.py
- [ ] Verify: PYTHONPATH=src .venv/bin/python -c "from metatron.mcp.server import mcp; app = mcp.streamable_http_app(); print('OK')"
- [ ] Verify: PYTHONPATH=src .venv/bin/python -c "from metatron.api.app import create_app; app = create_app(); print('OK')"
</success_criteria>

<output>
</output>
